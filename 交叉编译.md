1. 先初始化并激活本地环境
```bash
./scripts/bootstrap_env.sh
source scripts/activate_env.sh
```

2. 如果只想单独配置 ZCU102 交叉编译环境，可以直接运行：
```bash
./cmake_arm64.sh
```

3. 当前脚本会优先读取以下环境变量，而不是强依赖系统路径：
   - `NPUX_ENV_PREFIX`
   - `MLIR_DIR`
   - `LLVM_DIR`
   - `absl_DIR`
   - `CROSS_CXX`
   - `CROSS_CC`
   - `HOST_PY_INC`
   - `HOST_PY_LIB`
