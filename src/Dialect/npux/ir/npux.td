// Top-level TD for NPUX dialect: includes ops (which include the dialect).
// This file is the entry point used by add_mlir_dialect(npux npux).
#ifndef NPUX_TD 
#define NPUX_TD

include "npuxDialect.td"
include "npuxOps.td"

#endif // NPUX_TD
